# macro-micro-calc-cli
A python app that runs on the command line that calculates the recommended macros a person should consume based on the persons age, weight, height, sex, and activity level. Also based on user input, receive a list of DRIs for vitamins and minerals.
